1.文本一行居中，多行左对齐
.父元素{
 width:200px;
 text-align:center;
}
.子元素{
 display:inline-block;
 text-align:left;
}
*当文字为一行时，子元素的宽度小于父元素，子元素居中在父元素中，当大于一行时，子元素和父元素的宽度一致，文字就居左了。
*display:inline-block使子元素的宽度随文本的宽度伸缩
